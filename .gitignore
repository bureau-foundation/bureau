# Bureau .gitignore

# Agent tooling
/.claude

# Build system: Bazel (all builds and tests go through bazel build/test)
/bazel-*

# Nix build output symlinks
/result
/result-*

# Nix runner environment (symlink to /nix/store, machine-local)
deploy/buildbarn/runner-env

# Build artifacts (safety net for accidental go build at repo root)
/bureau
/bureau-*
*.exe
*.dll
*.so
*.dylib
*.test
*.out
*.prof
*.cover
coverage.txt
coverage.html

# Go workspace files (not committed; use Bazel for builds)
go.work
go.work.sum

# Python virtual environments
.venv/
venv/

# Bureau local state
/.notes/
*.log
logs/
tmp/

# Secrets
secrets/
.secrets/
*.key
*.pem
*.crt

# Deploy secrets (written by bureau matrix setup / attic bootstrap)
deploy/matrix/bureau-creds
deploy/matrix/.env-attic-token

# Beads (local workstream tracking)
.beads/
.bv/

# Local dev config
.envrc
.envrc.local
.env
.env-*
.bazelrc.local
*.local.yaml
*.local.json
*.code-workspace
history
.history/

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
