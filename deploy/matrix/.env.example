# Bureau Matrix Configuration
#
# Copy this file to .env and fill in the values.
# Generate a registration token with: openssl rand -hex 32

# Server name is permanent — it cannot change without wiping the database.
BUREAU_MATRIX_SERVER_NAME=bureau.local

# Registration token used by bureau-matrix-setup and the launcher to create accounts.
# Pass to bureau-matrix-setup via file, not CLI argument:
#   bureau-matrix-setup --registration-token-file deploy/matrix/.env-token ...
# Or via stdin:
#   echo "$TOKEN" | bureau-matrix-setup --registration-token-file - ...
BUREAU_MATRIX_REGISTRATION_TOKEN=

# TURN server configuration for WebRTC NAT traversal. The shared secret
# is used by both coturn (static-auth-secret) and Continuwuity (turn_secret)
# to generate time-limited HMAC credentials. Generate with: openssl rand -hex 32
BUREAU_TURN_SECRET=

# Hostname or IP where coturn is reachable. Defaults to localhost for
# single-machine deployments. Set to the machine's public IP or DNS name
# for multi-machine setups.
BUREAU_TURN_HOST=localhost
